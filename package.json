{
  "name": "queuesong",
  "version": "0.2.0",
  "private": true,
  "description": "Music queue jukebox - เพิ่มลิงก์เพลงจากหลาย platform, เล่นต่อเนื่องจนจบคิว, ต่อออกลำโพง/ทีวี (DLNA)",
  "main": "server.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "build:web": "cd web && npm install --no-audit --no-fund && npm run build",
    "start": "node server.js",
    "tunnel": "npx localtunnel --port 5173"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fast-xml-parser": "^4.4.1"
  }
}
